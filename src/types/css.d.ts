// Ambient declarations for side-effect style imports (e.g. `import "./globals.css"`).
// Next.js resolves these at build time; this keeps the editor's TS language
// server from flagging them when next-env.d.ts hasn't been picked up yet.
declare module "*.css";
